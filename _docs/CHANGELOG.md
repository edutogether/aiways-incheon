# 변경 이력

**무엇이 언제 왜 바뀌었는가.** 배포·수정이 나갈 때마다 한 줄씩 쌓는다.

- 자세한 근거와 시행착오는 커밋 메시지에, 앱 고유의 함정은 `.claude/rules/app.md`에,
  작업 전에 세운 계획은 `_docs/intents/`에 있다. 여기는 **연대기**다.
- 이 파일은 2026-09-09에 만들었다(문서 표준 §1). 그 이전 이력은 커밋 기록과
  `CLAUDE.md`에 남아 있던 결정들을 근거로 요약했다 — **날짜와 사실은 커밋에서 확인한
  것만 적었고, 확인 못 한 것은 적지 않았다.**

---

## 2026-09-11

### `miniapp/3second.html`(7차시 Vibe Coding 체험)이 **열흘 동안 죽어 있던 것**을 되살렸다

**2026-09-01 `2f90569`(GitHub Pages → Firebase Hosting)부터다.** 그 전까지 CSP는
`index.html` 안의 `<meta http-equiv>`였고, **meta CSP는 그 문서에만 적용되므로
`3second.html`에는 아무 CSP도 안 걸려 있었다.** 이전하면서 CSP를 `firebase.json`의
`hosting.headers`(`source: "**"`)로 옮기자 **모든 문서**에 걸리게 됐고, 이 파일은
그 CSP를 만족하도록 쓰여 있지 않았다. 인라인 스크립트와 `cdn.tailwindcss.com`이
둘 다 막혀 **화면은 뜨는데 버튼을 눌러도 아무 일이 안 났다.** 수업 중에 학생이
누르면 그냥 반응이 없는 상태였고, `index.html`의 "7차시 · Vibe Coding 체험하기"
카드에서 도달 가능했다.

🔴 **열흘 동안 아무도 못 본 이유: 배포가 이 파일을 검사하지 않았기 때문이다.**
CSP 차단은 **브라우저가 조용히** 하고 서버는 200을 준다. 스모크 검사도 CI도
초록불이었다. 그래서 이번에 되살리면서 **`scripts/checkMiniappCsp.js`**를 같이
넣었다 — 배포 전에 *실제로 나갈 바이트*로 인라인 해시를 계산해 `firebase.json`과
대조하고, 어긋나면 멈춘다. `deploy-hosting`이 `needs`로 잡는 `frontend` job에
걸어 두어 **어긋난 바이트가 라이브로 나가지 않게** 했다.

되살린 방법:

- **전역 CSP는 넓히지 않았다.** `source: "/miniapp/**"` 항목을 따로 두어 그 경로에만
  다른 `script-src`를 준다. 프로브 채널로 **실제 응답 헤더를 받아** 확인했다 —
  `/index.html`은 기존 값 그대로이고, miniapp 경로에서도 X-Frame-Options·
  Permissions-Policy·X-Content-Type-Options가 살아 있다. `script-src` 외 **9개
  지시자가 전역과 글자 그대로 같은 것**을 프로그램으로 대조했다.
- **인라인 이벤트 핸들러 21개를 `addEventListener`로 옮겼다.** 🔴 CSP에서 해시는
  `<script>` 블록만 허용하고 `onclick=` 같은 **속성은 별개로 막힌다** — 해시만
  넣었을 때 *스크립트는 도는데 버튼이 하나도 안 먹는* 상태가 실측으로 드러났다.
  `'unsafe-hashes'`로 핸들러마다 해시를 박는 방법은 **품목 문구가 한 글자만 바뀌어도
  그 버튼이 조용히 죽어서** 쓰지 않았다. 지금 CSP에는 `'unsafe-inline'`도
  `'unsafe-hashes'`도 없다.
- **죽은 웹폰트 `@import`를 지웠다.** `fonts.googleapis.com`이 `Pretendard`를
  **제공하지 않아** 그 요청은 HTTP 400에 `text/html`을 받고 브라우저(ORB)가 막는다
  (대조군 `Roboto`는 200 `text/css`). **처음부터 한 번도 동작한 적이 없었고** CSP와도
  무관하다. 화면은 이미 `sans-serif`로 떨어져 있었으므로 보이는 변화는 없다.
- `.gitattributes`에 **`miniapp/3second.html`과 `index.html`을 LF로 고정**했다.
  CSP 해시는 바이트로 비교하는데 로컬은 CRLF, CI는 LF라 **로컬에서 직접 배포하면
  해시가 어긋난다.** 새 검사가 실제로 그것을 즉시 잡아냈다 — `index.html`의 부트
  스플래시가 그 상태였다(라이브는 CI가 LF라 무사했다).

### HTML이 최대 한 시간 동안 옛 것으로 굳어 있던 것 — `Cache-Control: no-cache`

`firebase.json`에 캐시 규칙이 없어 **Firebase 기본값 `max-age=3600`**이 나가고 있었다.
여덟 저장소를 전수로 재보니 **이 앱만** 그랬다(나머지 일곱은 `no-cache`).

**왜 위험한가**: 브라우저가 최대 한 시간 동안 **서버에 묻지도 않고** 옛 `index.html`을
쓴다. 옛 HTML은 **옛 해시 번들**을 가리키므로, 배포 직후 들어온 사람이 "옛 HTML +
새 번들"이라는 **아무도 시험해 본 적 없는 조합**을 받을 수 있다. 🔴 더 나쁜 것은
되돌리기다 — `hosting:clone`으로 되돌려도 **이미 캐시된 브라우저에는 안 닿아서**,
되돌렸는데도 사용자는 계속 깨진 화면을 본다. 이 앱은 **카카오톡 인앱 브라우저**
비중이 큰데 거기는 강력 새로고침도 비공개 탭도 없어 **사용자가 캐시를 비울 방법이
사실상 없다.** S5(라이브 `index.html`이 전환본으로 바뀌는 작업) 직전이라 특히
그렇다 — 그래서 S5보다 먼저 고쳤다.

- `source: "/"`와 `source: "**/*.html"`에 `Cache-Control: no-cache`. 🔴 `no-cache`는
  "저장하지 마라"가 아니라 **"쓰기 전에 물어봐라"**다 — `no-store`면 매번 통째로 다시
  받는다.
- **HTML만 건드렸다.** 해시가 붙은 `bundle/**`과 `assets/**`는 그대로 뒀다.
  🔴 실측해 보니 이 저장소는 그것들도 **`max-age=3600`**이지 1년 불변이 아니었다 —
  긴 캐시 규칙이 애초에 없다. 그건 별개 판단이라 이번에 손대지 않았다.
- 프로브 채널에서 먼저 확인하고 나갔다: HTML 5경로(`/` · `/index.html` ·
  `/mobile/index.html` · `/admin.html` · `/miniapp/3second.html`) 전부 `no-cache`,
  HTML이 아닌 것은 그대로, 보안 헤더 5종과 miniapp CSP도 그대로.
- 🔴 **`scripts/postDeploySmoke.js`에 검사를 넣었다.** 헤더는 **조용히 되돌아가는**
  종류라(`firebase.json` 한 줄이면 사라지고 화면에는 아무 표시도 없다) 라이브 응답을
  보는 이 층에 둬야 한다. 넣자마자 **고치기 전 라이브에서 실제로 실패**하는 것을
  확인했다 — 검사가 실제로 무언가를 본다는 증거다.

#### 🔴 그리고 같은 날 `no-cache` → `public, max-age=60`으로 바꿨다

`no-cache`로 나간 뒤 **화면이 눈에 띄게 느려졌다는 보고**를 받고 실측했다. 두 가지를
그때 처음 알았고, **둘 다 문서 어디에도 없던 사실이다**:

1. 🔴 **`no-cache`는 브라우저뿐 아니라 CDN 엣지 캐시까지 끈다.** 같은 URL을 5회
   연속 요청했을 때 `no-cache`인 곳은 **다섯 번 다 `X-Cache: MISS`**였고,
   `max-age=3600`인 곳은 **2회차부터 `HIT`**였다. 대조로 같은 사이트의
   `style.css`(3600)는 `HIT`이 뜬다 — CDN은 멀쩡하고 이 지시자가 HTML만 엣지에서
   빼버린 것이다. **모든 방문이 원본 서버까지 간다.**
2. 🔴 **Firebase Hosting은 조건부 요청에 304를 주지 않는다.** `ETag`를 그대로 맞춰
   `If-None-Match`로 보내도, `If-Modified-Since`로 보내도 **`200` + 전체 본문**이
   온다(압축 13.7KB). 즉 `no-cache`가 "쓰기 전에 물어보기"가 아니라 **"매번 전체
   재다운로드"**가 된다.

**60초를 고른 이유**: CDN HIT을 되찾아 속도를 회복하면서도, **되돌리기가 1분 안에
사용자에게 닿는다.** 1시간과 1분은 사고 대응에서 완전히 다른 값이고, `no-cache`를
넣은 원래 이유(옛 HTML + 새 번들)도 1분이면 실질적으로 해결된다.

`postDeploySmoke.js`의 검사도 같이 바꿨다 — 이제 **`max-age`가 300초를 넘지 않는지**를
본다. 🔴 값이 빠지거나 커지는 쪽(예: 규칙이 지워져 기본값 3600으로 돌아가는 것)을
막는 것이 목적이라 **상한으로** 못박았다.

### 스플래시가 **최소 두 바퀴**는 보이게 — PC에만 하한이 없었다

Bumm님이 PC 화면에서 "스플래시가 안 보일 정도로 스쳐 지나간다"고 하셨다. 재보니
**0.1~0.9초**였다. 모바일에는 하한이 있었는데(`MINIMUM_TURNS = 2`, 9/10에 넣음)
**PC에는 없었다** — 5초 상한과 470ms 페이드만 있고 바닥이 없었다.

🔴 **한쪽에 넣은 하한은 다른 쪽에 가지 않는다.** 기기마다 따로 넣고 따로 잰다.

- 하한은 "몇 초"가 아니라 **자기 애니메이션 두 바퀴**다 — `getAnimations()`로 실제
  주기를 읽어 계산하므로 막대 주기를 바꾸면 하한이 따라온다.
- **상한(5초)은 그대로다.** 목적이 달라 따로 정한다. 부팅 실패를 흉내 내(`app.js`를
  막아) **5,461ms에 걷히는 것**을 확인했다.
- **태블릿은 PC와 같은 스플래시를 탄다** — 실측 768×1024 2,681ms / 1180×820 2,660ms
  (PC 2,676ms). 따로 넣을 것이 없다.
- 🔴 **CSP 해시를 같이 갱신했다.** 그 인라인 스크립트는 해시로 허용되는 자리라 한 글자만
  바꿔도 브라우저가 조용히 막는다. `scripts/checkMiniappCsp.js`가 **실제로 잡아서 멈춰
  세웠다** — 오늘 그 게이트를 만든 이유가 이 자리였다. 사본이 둘(`index.html`,
  `pc-app/index.html`)이라 바이트까지 같게 고쳤다.

실측(학교 미설정 · warm 캐시 · 스로틀 없음): **PC 2,835ms(2.47바퀴) · 태블릿 세로
2,720ms · 태블릿 가로 2,920ms · 학생 앱 2,228ms(2.03바퀴)**.

`tests/splashFloor.spec.js`가 **상수가 아니라 화면에 보인 시간을 잰다.** 하한을 0으로
낮춰 빨간불이 뜨는 것까지 확인했다(PC 524ms · 학생 앱 423ms).

### 학교 검색이 **타자 치는 대로** 좁혀진다 — 전국 목록을 브라우저에 담았다

타자마다 서버를 거쳐 느렸다(실측 **콜드 1.89초 / 웜 0.16초**). Bumm님 요구는 "타자
치는 대로"였고 **서버를 거치는 한 그건 나올 수 없다.** 전국 학교 **12,672개**를
`assets/school-list.tsv`로 굳혔고, 검색이 브라우저에서 **0.4~1.5ms**에 끝난다.

- **부분 일치 · 띄어쓰기 무시 · 초성**(`ㅇㅊㅅㅎ` → 인천서흥…)까지 된다. 순서는
  완전 일치 > 앞부분 일치 > 부분 일치 > 초성 앞부분 > 초성 부분.
- **화면은 한 글자도 안 바뀐다.** 정적 목록과 서버 폴백이 `renderSchoolResults()`
  **하나로** 그리고, 두 경로의 필드가 완전히 같은 다섯인 것을 확인했다. 급별 표기
  (`인천동방초등학교 (초등학교)`)와 **주소가 빈 학교 58개가 시도로 떨어지는 동작**도 그대로다.
- **`searchSchool`은 유지한다 — 이제 폴백이라는 진짜 역할이 있다.** 목록이 아직
  안 왔거나 못 받았으면 그쪽으로 간다. 느린 회선에서도 **지금보다 나빠지지 않는다.**
- 🔴 **초성 색인은 파일에 굳히지 않았다** — 브라우저에서 만들면 **4.6ms**, 파일에
  굳히면 **brotli +31.2KB**다.
- 🔴 **미리받기 조건 셋을 실측으로 지켰다**: 학생 앱에서 요청 **0건**(학생은 학교
  검색을 쓰지 않는데 학교 와이파이에서 164KB를 받게 할 수 없다) · **아낌 모드/2G면
  0건**(폴백으로 간다) · **첫 화면 지표가 있음/없음에서 차이 없음**(FCP 312 vs 312ms,
  long task 양쪽 0개).
- **캐시**: `/assets/school-list.tsv` **하나만** 1년 불변. 🔴 `assets/**`로 넓히지
  않았다 — 그 안에 QR·로고처럼 **이름이 그대로인 채 내용만 바뀌는** 파일이 있고, 거기에
  1년을 주면 QR을 바꿔도 1년 동안 옛 QR을 보는 사람이 생긴다. 목록은 내용이 바뀌면
  `app.js`의 `?v=` 해시가 **같이** 바뀌므로 안전하다(스크립트가 못 고치면 멈춘다).
- 만드는 것은 `scripts/fetchSchoolList.js`. 받은 행이 NEIS 보고와 다르거나 10,000개
  미만이면 **멈춘다** — 조용히 반쪽 목록이 굳으면 그 학교 선생님이 아예 못 들어간다.

### 그 밖

- **Hosting 프리뷰 채널 오리진을 CORS에 허용**했다. S5 전환본을 라이브와 같은
  조건에서 확인하기 위한 것이고, 좁은 정규식으로 이 프로젝트 채널만 잡는다.
  🔴 S5가 끝나면 정규식·테스트·`scripts/stageReviewChannel.js`를 같이 뺀다.
- **`pc-frontend` CI job이 `a8d2f5c`(9/10)부터 계속 실패**하고 있던 것을 고쳤다.
  PC 빌드가 gitignore된 `mobile/`을 요구하는데 그 job이 만들지 않아서였다.
  요구사항을 없애지 않고(그 검사는 폰 폭 404 빈 프레임을 잡은 장치다) job이
  조건을 채우게 했다. `deploy-hosting`이 이 job을 `needs`로 잡지 않아 배포는
  계속 나갔고, 그만큼 눈에 안 띄었다.
- **태블릿은 교실 전광판이라 내비게이션이 없는 것이 의도**임을 확정하고
  `app.md`에 못박았다(실측 표 포함). QR도 태블릿에서 이미 첫 화면에 있다 —
  아이패드 전 크기(12.9" 양 방향 포함)를 뷰포트 기준으로 재서 확인했다.
  경계는 `87.4rem` = 1398.4px다.

## 2026-09-09

하루에 두 가지 큰 것이 나갔다: **모바일 앱 리액트 전환(라이브 교체까지)** 과
**도메인 정리**.

### 모바일 앱을 React + TypeScript로 전환 (S0~S5)

`mobile/`은 이제 손으로 쓰는 폴더가 아니라 `mobile-app`의 **빌드 산출물**이다.
손으로 쓴 1,850줄(`app.js` 1,274 / `index.html` 456 / `sortingData.js` 370 /
`sortingVision.js` 122 / `sortingTextTip.js` 90)이 사라졌다.

절대 기준은 **"화면과 체감이 지금과 같아야 한다"** 였다. 그래서 옮기기 전에 기준선을
만들어 고정해 두고, 옮긴 뒤 **그 파일을 그대로 둔 채 통과**시켰다 — 새로 찍어 맞춘 것이
아니다. 4개 뷰포트 × 13개 상태 × (좌표·모션 선언값·화면에 안 보이는 값·스크린샷).

| 커밋 | 무엇 |
|---|---|
| `ff7c269` | 착수 전 intent(등급 2) 작성 — 무엇을 어떻게 증명할지 먼저 정함 |
| `fca5002` | 🔴 **기준선이 실제로는 아무 값도 재고 있지 않던 것 수정.** `page.evaluate(fn, [A, B])`가 배열을 인자 하나로 넘기는데 함수가 둘로 받아서, 3주 가까이 빈 문자열을 기록하고 있었다 |
| `b7ab66c` | 상호작용 9개 상태(모달·퀴즈 정오답·보류함·토스트·자동완성·담임가입)까지 기준선에 고정 |
| `128c91f` | 토대 — `mobile-app`에 React 18 + TS + Vite |
| `305a5f2` | 마크업 456줄 → TSX (className 문자열 그대로) |
| `447c256` | 데이터·판정 로직 582줄 → 타입 붙인 TS. 원본을 실제로 실행해 값을 통째로 대조 |
| `a06f07e` | 화면 로직 1,274줄 → 훅 |
| `2f0e105` | **라이브 교체.** 전환 전 상태는 태그 `mobile-react-freeze-20260909` |
| `29a1063` | 기준선 하네스가 앱 렌더보다 먼저 인증 게이트를 열던 것 수정 |
| [PR #2](https://github.com/edutogether/aiways-incheon/pull/2) | README 정리 + 전환 기록 |

되돌리는 법은 `_docs/intents/2026-09-09-mobile-react-conversion/intent.md`에 있고,
**프리뷰 채널에 실제로 되돌려 화면까지 확인**했다.

### 교사 인증코드 — 한 반이 코드를 여러 개 받게 (`09eb971`)

규칙 코드(`SEOHEUNG103`)와 담임 개인 코드(`EDU2G SANGHYUN`)가 **같이** 통한다.
대체로 만들면 개인 코드에 문제가 생겼을 때 그 선생님이 들어갈 길이 없어진다.
비교 전에 **공백을 전부 지우고 대문자로 맞춘다** — 띄어쓰기 하나로 실패가 쌓이면
그 반 전체가 15분 잠기기 때문이다.

### 검사 건전성 — "대상을 못 찾으면 조용히 통과" 7건 수정 (`96f0a3b`)

배포 스테이징이 없는 파일을 조용히 건너뛰던 것(화면이 사라져도 배포 성공),
기준선이 탭 버튼을 못 찾으면 직전 화면을 정답으로 찍던 것, 빈 덤프가 기준선에
들어앉을 수 있던 것 등. **그 자리에서 새로 넣은 문지기 하나가 같은 병에 걸린 것도
되돌림 확인에서 잡아 고쳤다** — 문지기가 감시 대상에서 기준을 가져오면 감시가 되지 않는다.

### 가입 승인 게이트 제거, 교사 사후 조치로 전환 (`9041af9`, `47b6715`, `a67b90a`)

승인 대기 없이 바로 가입되고, 대신 담임이 자기 반 학생의 계정을 차단하거나 기록을
지울 수 있다. `a67b90a`에서 즉시 가입 경로에 빠져 있던 커스텀 클레임 설정을 복구했다 —
빠진 채로 두면 새 학생의 대시보드가 실시간 구독 대신 폴링으로 떨어진다.

### 도메인 정리

- `17fb178`, `88c72be`, `ab319f1`, `fb14e5b` — QR·og 태그·모델 주소가 폐기된 GitHub
  Pages를 가리키던 것을 `incheon.edutogether.kr`로 교체
- `0311c0e` — 새 도메인의 CORS 허용목록 반영. **정적 파일은 정상인데 Functions 호출이
  전부 403이던 것**을 배포 후 스모크가 잡았다
- `278fc06` — 옛 `web.app` 주소를 허용목록에서 제외(클로즈베타 재시작). 그 주소는
  화면만 뜨고 API는 거부되는 것이 의도한 상태다

### 참여 학급 수 확정

**청라초 3학년 9개 반 전부, 마전초 6학년 7개 반 전부**(서흥초 1학년 3반, 동방초
5학년 4반). `app.js`의 `SCHOOL_CLASS_CONFIG`에 이미 들어 있던 값과 일치해
코드 변경은 없었고, 확정 사실로 `.claude/rules/app.md`에 적었다 — 다음 세션이
또 묻지 않게 하기 위해서다.

### 관리자 화면 — 없는 반은 아예 못 고르게

학년·반을 숫자로 직접 치던 것을 고르는 방식으로 바꿨다. 없는 반의 코드가
발급되면 **아무도 쓸 수 없는 코드**가 생기는데, 현장에서는 왜 안 되는지
알 방법이 없다. 학급 수는 **NEIS 학급정보 API**에서 받아 `schoolClassCounts.js`에
굳혔다 — 받은 시각과 출처가 파일 안에 같이 있다. 자료에 없는 학교는 넓게
보여주되 "확인되지 않았다"고 화면에 드러내고, 자료 자체를 못 읽으면 **빈 목록을
보여주는 대신 발급을 막는다.**

### 그 밖

- `2e7f79a` — 반 선택을 학년 선택 뒤에, 그 학교·학년에 실제로 있는 반만
- `ee0cb1e` — 교사코드를 학교·학년·반에서 규칙으로 자동 생성
- `8db24c8` — App Check 라이브 확인을 자동화로 시도 → **불가**(대조군도 함께 막힘 =
  설정 문제가 아니라 자동화의 한계). 검사는 남겨 뒀다

---

## 2026-09-08

리액트 전환 전 화면을 고정하는 **비주얼 기준선 하네스** 추가. 루트
`package.json`에 `"type": "module"`을 넣었다가 `functions/test`의 `require()`가
깨져 되돌린 것도 이날이다.

## 2026-09-07

종합감사. `functions/package.json`의 `check` 하드코딩 목록에서 9개 파일이 빠져 있던 것,
교사코드 반 단위 실패 잠금(10분에 10회 → 15분 차단) 추가.

## 2026-09-02

가입 경로 통합 — 학생과 담임이 **하나의 가입 화면**에서 역할을 고르는 방식으로.
담임을 고르면 번호 칸이 인증코드 칸으로 바뀐다.

## 2026-09-01

**GitHub Pages → Firebase Hosting 이전.** 배포 파일을 `scripts/stageHostingSite.js`가
**화이트리스트로** 골라 담는 방식으로 바꿨다 — Pages 시절 `path: .` 설정으로 저장소
전체가 공개됐던 사고를 반복하지 않기 위해서다. 교사코드 해싱도 이때 sha256에서
scrypt+솔트로 바꿨다(사람이 고른 짧은 문자열이라 빠른 해시가 사전공격에 취약).

## 2026-08-31

**3단 권한체계** 구현 — 교사코드 인증, 가입 승인 대기열, 학교잠금 교정, 슈퍼어드민
(이 앱 최초의 실제 로그인), 반 전체 CSV 내보내기.

## 2026-08-26

**Firestore 단일 백엔드로 완전 전환.** Google Sheets/Apps Script 연동 제거,
MobileNet 제거(Gemini 단독 판정).

## 2026-08-19 ~ 08-25

**백엔드 전면 재설계.** 8/19에 "실제로 작동하는 시스템이 아니었음"이 드러난 것이
출발점이었다 — 모바일 앱에 네트워크 호출이 0줄이었고, 화면에 뜨는 최종 판단을
Gemini가 아니라 MobileNet이 정하고 있었다. 상세 경위는 `_docs/ops/handoff.md`.

---

## 복구 지점 (freeze 태그)

`.githooks/pre-push`가 이 태그들의 삭제·이동을 막는다. 각 태그의 설명은
`git show <태그>`로 볼 수 있다.

| 태그 | 무엇을 고정했나 |
|---|---|
| `mobile-react-freeze-20260909` | 리액트 전환 직전 라이브 상태(`mobile/`이 손코드였을 때) |
| `aiways-incheon-freeze-20260908-pre-react` | 전환 착수 직전 |
| `aiways-incheon-freeze-20260907-pre-vitest` | 테스트 도구 교체 직전 |
| `aiways-incheon-freeze-20260826` | Firestore 단일 백엔드 전환 시점 |
| `mobile-freeze-20260812` | 모바일 앱 전방위 검토 시점 (⚠️ "모바일 동결"이라는 뜻이 아니다) |
| `pc-expo-freeze-*`, `pc-glass-redesign-freeze-20260819`, `pc-post-expo-freeze-20260818` | PC 화면 각 시점 |
