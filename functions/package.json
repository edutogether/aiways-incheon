{
  "name": "aiways-sorting-functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "test": "node --test test/sortingVision.test.js",
    "check": "node --check index.js && node --check lib/sortingVision.js && node --check lib/sortingVisionSchema.js && node --check test/sortingVision.test.js && node --check test/emulatorSmoke.js && node --check test/browserSmoke.js",
    "serve": "firebase emulators:start --only functions --project demo-aiways-incheon"
  },
  "dependencies": {
    "@google/genai": "^1.14.0",
    "firebase-functions": "^6.5.0"
  }
}
