"use strict";
const assert = require("node:assert/strict"), fs = require("node:fs"), path = require("node:path"), test = require("node:test");
const root = path.resolve(__dirname, "..", ".."), html = fs.readFileSync(path.join(root, "index.html"), "utf8"), tuner = fs.readFileSync(path.join(root, "visualTuner.js"), "utf8"), css = fs.readFileSync(path.join(root, "styles", "cb3.css"), "utf8");
test("CB-3 keeps the eight-section contract and loads only presentational recovery assets", () => { const ids = [...html.matchAll(/<section[^>]+id="([^"]+)"/g)].map(match => match[1]); assert.deepEqual(ids, ["dashboard", "project", "curriculum", "hah", "flow", "gallery", "sorting", "resources"]); assert.ok(html.indexOf("./visualTuner.js") < html.indexOf("./aiRuntimeLoader.js")); assert.match(html, /styles\/cb3\.css/); assert.match(css, /--scene-content-max/); assert.match(css, /@media \(max-width: 41\.99rem\)/); });
test("CB-3 visual tuner is localhost-query gated, allowlisted and storage-namespaced", () => { assert.match(tuner, /localhost/); assert.match(tuner, /127\.0\.0\.1/); assert.match(tuner, /visual-tuner/); assert.match(tuner, /aiways\.visualTuner\.v1/); assert.match(tuner, /dashboard hero/); assert.match(tuner, /EDU2G trigger/); assert.doesNotMatch(tuner, /fetch\(|XMLHttpRequest|Firebase|actorId|idToken|managementId/); });
