# External Health Review — 2026-08-10

Source: cross-project review run from the `D:\Project` parent-folder Claude session (not this project's own session). Read-only investigation — no code was changed, no worktrees removed. Bring this into this project's own session to decide what (if anything) to act on.

## Method
Same rubric used on a sibling project (codyssey), adapted for a no-build vanilla-JS + Firebase Functions stack: file-size scan, TODO/FIXME/HACK/console.log grep, test presence + actually running the suite, Firestore rules read, secrets-in-tree check, git/CI state — plus a full `git worktree list` audit, since this repo's checkout situation turned out to be the headline finding.

## Findings (ranked)

1. **SEVERE — 21-worktree sprawl, and the "canonical" folder is not the freshest work.** `git worktree list` on this repo shows the checkout at `D:\Project\ssamkang\aiways-incheon` (branch `feature/owner-visual-recovery`, HEAD `5bb0d65`, 2026-08-02) plus **20 other worktrees** of the same repo scattered across `D:\Project\ssamkang\aiways-incheon-*` (6 more, including `wip/cb3b-frontend-handoff-20260803` at HEAD `5c6ddcf` dated **2026-08-09 22:29 — more recent than this "canonical" folder and than `main`**), `D:\Project\_aiways-design-candidates-20260806\{A..E}_*` (5 detached, named like a design bake-off: "FINAL_UI_POLISH", "FINAL_PRODUCTION", "DASHBOARD_REFINED", "DASHBOARD_POLISH", "PRE_REFACTOR"), `D:\Project\_visual-recovery\aiways-candidate-d` (1 detached), and 8 more under `C:\Users\817be\AppData\Local\Temp\aiways-visual-history\*` / `...review-worktree-*` (detached, hash-named — pure scratch). Several commits are checked out redundantly 2-3× in different locations (e.g. `0bb2443` exists simultaneously in three places; `741206d` in three places). Reads like an automated visual-diff/screenshot-comparison workflow (`functions/test/frontendLayoutSmoke.js` and siblings drive real Chrome via CDP) that was never torn down. **Practical risk:** anyone opening `D:\Project\ssamkang\aiways-incheon` cold is looking at 7-day-stale code, not the latest work — the freshest commit is in `aiways-incheon-closed-beta`. The Temp-folder and `_aiways-design-candidates`/`_visual-recovery` worktrees look safe to `git worktree remove` once no longer needed for the visual comparisons they were made for.
2. **No frontend unit tests.** `app.js` (211,770 bytes / 4,377 lines / 201 functions, one top-level IIFE, no internal module/section structure) has zero direct unit-test coverage — only indirect Chrome-DevTools-Protocol-driven visual/DOM-contract "smoke" scripts (`frontendLayoutSmoke.js`, `frontendVisualAlignmentSmoke.js`, `frontendBrowserMatrixSmoke.js`, etc., checking overflow/tap-target-size/interaction contracts across 40+ viewport widths — these are real regression checks, not stubs, but they don't test `app.js`'s logic in isolation).
3. **Tests exist but aren't gated in CI.** `.github/workflows/deploy-pages.yml` triggers only on `workflow_dispatch` (manual) — no `push`/`pull_request` trigger runs `functions/`'s test suite before a GitHub Pages deploy. A broken Cloud Function could ship without any automated check catching it first.
4. **`app.js` is a single 4,377-line file with no internal boundaries** — the file-level equivalent of a god-component. Not a problem today, but will get harder to navigate as it grows.
5. Positives, and they're substantial: zero TODO/FIXME/HACK/XXX/WORKAROUND and zero leftover `console.log` anywhere checked. `functions/` has a real backend test suite — 24 files, 1,048 LOC, 187 `assert.*` calls via Node's built-in `node:test`, with dependency-injected mocks (e.g. an in-memory `db = new Map()` store) covering idempotency and 9 malformed-payload rejection cases. `firestore.rules` is a deliberate `allow read, write: if false` for all client SDK access — every read/write instead goes through `onRequest` Cloud Functions using the Admin SDK, gated by App Check verification (`functions/lib/appCheckProtection.js`) plus rate-limiting and idempotency checks — arguably a more locked-down posture than a permissive-looking client-facing ruleset. Secrets hygiene is clean: the embedded Firebase web `apiKey` in `firebaseAppCheck.js:2` is a public-by-design key (access is gated by App Check, not key secrecy); real secrets (`GEMINI_API_KEY`, `EDU2G_PASS_REGISTRY_JSON`) are wired via Secret Manager, not committed; `appsScriptUrl` values are empty placeholders in the tracked files. Repo is confirmed public (`gh repo view` → `isPrivate: false`) with no service-account/private-key material found via grep. `docs/` holds 7 dated "Stage N" audit files — a genuine internal changelog trail.

## Bottom line
The code itself (frontend + functions) is more disciplined than the folder situation suggests — clean markers, a real backend test suite, a maximal-deny Firestore posture backed by App Check/rate-limiting. The dominant risk here is **organizational, not code-quality**: 21 scattered worktrees of the same repo, with duplicated commits and a "main" folder that isn't actually the newest code, is the kind of thing that makes it genuinely unclear — even to the project's own owner — which of 6+ `aiways-incheon-*` folders is the real one to work in.
