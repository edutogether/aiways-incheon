// 리액트가 화면을 그린 **뒤에** app.js를 부르겠다고 미리 알리는 깃발.
//
// 🔴 왜 별도 파일인가: `firebase.json`의 CSP가 인라인 <script>를 **sha256 해시로만**
// 허용한다. 인라인을 하나 더 만들면 그 해시까지 같이 갱신해야 하고, 틀리면
// 브라우저가 **조용히** 실행을 막는다 - CI에서도 안 보이는 실패다. 외부 파일은
// script-src 'self'로 이미 허용된다.
//
// 🔴 왜 필요한가: `app.js`는 DOMContentLoaded에 `getElementById`로 DOM을 붙잡는데,
// 리액트는 그보다 **뒤에** 그린다(번들이 defer 고전 스크립트 뒤에 오고,
// `createRoot().render()`는 동기 완료가 보장되지 않는다). 그대로 두면 app.js가
// 채울 DOM을 못 찾고 화면이 빈 채로 남는다. 타이밍에 기대지 않고 **리액트가
// 직접 부르게** 하려면, app.js가 자동 실행을 건너뛸지 미리 알아야 한다.
//
// 🔴 이 파일은 전환본(pc-next)에만 실린다. 원본 `index.html`에는 없으므로 깃발이
// 서지 않고, 라이브는 예전처럼 자동 실행된다 - S5까지 그대로 돌아야 한다.
window.__AIWAYS_PC_REACT_BOOT = true;
