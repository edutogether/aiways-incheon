import {createServer} from 'node:http';import {readFile,stat} from 'node:fs/promises';import path from 'node:path';export function serve(root,port){return createServer(async(q,r)=>{let p=path.join(root,new URL(q.url,'http://x').pathname.replace(/^\/aiways-incheon\//,''));try{if((await stat(p)).isDirectory())p=path.join(p,'index.html');r.end(await readFile(p))}catch{r.statusCode=404;r.end()}}).listen(port,'127.0.0.1')}
