{
  "name": "aiways-sorting-functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "test": "node --test test/sortingVision.test.js test/sortingRecord.test.js",
    "check": "node --check index.js && node --check lib/sortingVision.js && node --check lib/sortingVisionSchema.js && node --check lib/sortingRecord.js && node --check test/sortingVision.test.js && node --check test/sortingRecord.test.js && node --check test/emulatorSmoke.js && node --check test/recordEmulatorSmoke.js && node --check test/browserSmoke.js",
    "serve": "firebase emulators:start --only functions,firestore --project demo-aiways-incheon",
    "emulator:test": "firebase emulators:exec --only functions,firestore --project demo-aiways-incheon \"node test/recordEmulatorSmoke.js\""
  },
  "dependencies": {
    "@google/genai": "^1.14.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.5.0"
  }
}
